% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{do_primer_search}
\alias{do_primer_search}
\title{Search and remove primer sequences}
\usage{
do_primer_search(
  fq_paths,
  amplicon_primers,
  out_dir,
  primer_max_err = 0.2,
  idx_max_diffs = 0,
  min_barcode_length = 50,
  cores = 1
)
}
\value{
Returns trimmed FASTQ paths (Zstandard-compressed) and statistics:
\code{list(trimmed_fq = c(...), stats = list(...))}. More FASTQ files
are generated for reads without primers/indexes or reads that are too short
or potential concatenated products.
}
\description{
Requires \href{https://github.com/markschl/seqtool}{seqtool} v0.4 or higher

See \link{do_trim_demux} for more details on the arguments
}
\details{
The function generates different FASTQ files in \code{out_dir},
whereby \emph{trimmed.fastq.zst} contains "valid" reads with sample indexes in the
sequence headers: \verb{<id> fi=<fwd-idx-name> ri=<rev-idx-name>}
}
